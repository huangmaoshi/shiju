# 「拾句」作文素材与金句摘抄库 APP 产品设计文档

**文档版本**：V2.0
**产品定位**：面向学生群体的轻量化作文素材工具 + 金句卡片生成器
**核心优势**：素材全、分类准、AI 金句提取、定时自动采集、一键卡片化、无冗余功能
**后端定位**：轻量服务端 + 内置 AI 提取 + 内置 GitHub 数据采集 + 内置 Cron 调度

---

## 一、产品概述

### 1.1 产品背景

初高中、大学生群体长期存在作文素材积累、文案摘抄需求，但现有产品普遍存在三个痛点：

- 素材杂乱无分类，找对应主题的金句效率极低
- 只有文字内容，无适配社交平台的可视化输出
- 功能臃肿、广告泛滥，纯工具体验差
- 优质金句需要自己从原文里抠，没有 AI 辅助提取

「拾句」以「精准素材 + AI 自动提取金句 + 一键卡片化 + 定时自动采集」为核心，主打干净、高效、高颜值。

### 1.2 目标用户

| 用户群体 | 典型场景 |
|---|---|
| 初高中学生（12-18 岁） | 高考/中考作文素材积累、考前押题素材包 |
| 大学生（18-22 岁） | 四六级/考研作文、日常文案摘抄 |
| 文案爱好者 / 教师 | 备课素材参考、灵感收集 |

### 1.3 核心价值

- **精准分类素材库**：按作文主题、内容类型双维度分类，标注适用场景
- **AI 金句自动提取**：大模型从原文里批量挖掘高质量金句，自动去重
- **定时自动采集**：50+ 合规数据源，Cron 定时采集入库
- **原文审核机制**：AI/采集的内容先入库待审，人工过审才对用户展示
- **零门槛金句卡片**：多套精美模板，一键生成高清配图
- **全场景摘抄体验**：每日推荐、随机灵感、收藏管理、自建金句、云同步

---

## 二、整体功能架构

```
拾句 APP
├── 首页模块（每日推荐 + 随机摘抄 + 快捷入口）
├── 素材库模块（全量素材 + 分类检索 + 作文模板）
├── 卡片工坊模块（模板选择 + 自定义编辑 + 导出分享）
├── 我的收藏模块（收藏夹 + 自建金句 + 背诵计划）
├── 个人中心模块（微信登录 + 会员中心 + 设置）
│
└── 管理后台（Web）
    ├── Dashboard 数据总览
    ├── 用户管理（创建/编辑/封禁/会员）
    ├── 金句管理（CRUD + 启用开关 + 选段生成）
    ├── 原文管理（录入 + 选段一键加金句）
    ├── AI 金句提取（多模型配置 + 批量任务 + 自动去重）
    ├── 采集管理
    │   ├── 采集源管理（50+ 预设源）
    │   ├── 手动导入（GitHub Raw 直导入）
    │   └── 定时采集调度（Cron 表达式 + 启停 + 立即执行）
    ├── 审核管理（批量审核通过/拒绝）
    ├── 分类管理（多级父子）
    ├── 广告配置
    ├── 卡片模板管理
    └── 系统配置
```

---

## 三、核心功能设计

### 3.1 素材库

#### 分类体系

双维度 + 标签标注：

| 维度 | 具体分类 |
|---|---|
| **内容类型** | 古诗词曲 / 现代散文 / 名人名言 / 歌词台词 / 现代诗歌 / 人物素材 |
| **作文主题** | 青春成长、家国情怀、思辨哲理、文化传承、奋斗坚持、情感共情、科技创新、社会观察 |
| **适用场景** | 开头金句、结尾升华、过渡衔接、论点论证 |

#### 选段一键加金句（差异化亮点）

原文详情页支持**文本选择 → 浮动气泡「加为金句」**的交互：
1. 用户在原文页选中任意文本（浏览器 selection API）
2. 页面弹出浮动气泡「+ 加为金句」
3. 点击后弹出表单：自动带入选中文本 + 原文作者/出处，可编辑后入库
4. 系统自动计算 **MD5 指纹** 并与金句库比对，避免重复

### 3.2 AI 金句提取（差异化亮点）

#### 支持的大模型

后端采用 **OpenAI 兼容协议**，前端提供一键预填：

| Provider | Base URL （自动预填） | 典型模型 |
|---|---|---|
| **火山方舟（豆包）** | `https://ark.cn-beijing.volces.com/api/v3` | doubao-1-5-pro / doubao-1-5-lite / doubao-pro-32k |
| OpenAI | `https://api.openai.com/v1` | gpt-4o / gpt-4o-mini |
| 阿里百炼 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | qwen-plus / qwen-turbo |
| 百度千帆 | `https://qianfan.baidubce.com/v2` | ernie-4.0-turbo |
| 智谱 | `https://open.bigmodel.cn/api/paas/v4` | glm-4 |
| DeepSeek | `https://api.deepseek.com/v1` | deepseek-chat |
| Moonshot (Kimi) | `https://api.moonshot.cn/v1` | moonshot-v1-8k |
| 自定义 | 手填 URL | 任意兼容 OpenAI 的模型 |

#### 核心流程

```
管理员在后台选原文（可多选 / 全选）
        │
        ▼
选择 AI 配置（Provider + 模型 + API Key）
        │
        ▼
后端异步任务：逐篇发送给大模型 → 解析返回的金句列表
        │
        ▼
自动 SimHash 去重（与已有金句库比对，相似度 >85% 则跳过）
        │
        ▼
新金句入库（quoteType=aiExtracted, auditStatus=0 待审）
        │
        ▼
审核管理 → 批量通过 → 对用户展示 ✅
```

**设计原则**：
- 提取结果**自动去重**，避免污染已有金句库
- AI 提取的金句默认**待审状态**，管理员审核通过后才展示
- 后台实时显示提取进度和去重跳过数量

### 3.3 数据自动采集（差异化亮点）

#### 采集方式

| 方式 | 适用场景 | 特点 |
|---|---|---|
| **GitHub Raw 直导入** | 公开 JSON 格式的数据集仓库 | 30s 超时 + 3 次重试 + 中文路径自动 URL 编码 |
| **定时自动采集** | 需要周期性刷新的数据源 | Cron 表达式配置、任务启停、立即执行 |

#### 50+ 预设采集源

覆盖六大品类：

| 品类 | 示例数据源 |
|---|---|
| 古诗词 | chinese-poetry（唐诗/宋词/诗经/楚辞/元曲）、poetry-dataset（明/清）|
| 现代散文 | prose-collection / chinese-prose |
| 名人名言 | Wisdom-Quotes / Chinese-quotes / Quote-Garden |
| 现代诗歌 | modern-chinese-poetry |
| 影视台词 | old-chinese-movie-scripts / soviet-movie-scripts |
| 歌词 | lyric-collection |
| 人物素材 | 鲁迅 / 朱自清 / 徐志摩 / 老舍 / 林徽因 / 沈从文（公版节选）|

每个采集源标注：`complianceTag`（合规标签）+ `protocol`（协议类型）+ `datasetType`（品类）+ 预估数据量。

#### 定时采集调度器

后端 Cron 调度器（node-cron）：

```
后端启动 → CronScheduler.startAll()
  → 从 DB 读取所有 CrawlSchedule（enabled=1）
  → 按 cronExpr 注册 node-cron job
  → 到点自动执行 github_import_service
  → 更新 lastRunAt / lastRunStatus / lastRunCount
```

管理后台 → 采集管理 → ⏰ 定时采集 Tab：
- 为每个采集源配置**独立的 Cron 表达式**（5 段：分 时 日 月 周）
- 每个任务可**单独启停**
- 支持「立即执行」按钮（不等 Cron 到点）
- 实时查看下次运行时间和上次执行结果

#### 分级去重机制

所有采集/AI 提取的数据入库前都会经过三重去重：

| 层级 | 方法 | 阈值 | 比对范围 |
|---|---|---|---|
| ① 批次内 MD5 | 同一批数据内先算一次 | 精确匹配 | 当前批次 |
| ② 正式库 MD5 | `Quote.contentMd5` / `OriginalText.contentMd5` | 精确匹配 | 正式库 + 临时库 |
| ③ 长文本 SimHash | 文本长度 ≥ 100 字才执行 | 汉明距离 ≤ 10 → 相似度 > 85% | 正式库 + 临时库 |

### 3.4 原文审核流程

**设计原则**：所有非管理员手动录入的数据（采集 / AI 提取），默认都是**待审状态（auditStatus=0）**，不对用户展示。只有管理员审核通过后才在用户端出现。

```
                    ┌──────────────────┐
  采集 / AI 提取 ─►  │ auditStatus = 0  │  ──►  用户端查询 WHERE auditStatus=1
                    │  待审（不对用户展示）│       待审原文不可见 ✅
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │  管理后台审核管理   │
                    │  ✅ 批量通过       │
                    │  ❌ 批量拒绝       │
                    └────────┬─────────┘
                             │
                             ▼
                    auditStatus = 1（通过）
                    → 用户端可见 ✅
```

**审核管理页面**：
- 支持按状态筛选（待审 / 已通过 / 已拒绝）
- 批量选择 + 一键通过 / 拒绝
- 可按采集源 / 采集时间 / AI 配置过滤

---

## 四、变现方案（与之前一致，未变）

### 4.1 广告变现（非会员）

| 广告场景 | 类型 | 触发 |
|---|---|---|
| 长素材全文解锁 | 激励视频（15s） | 超过 2 行的散文/人物素材 |
| 作文模板完整解析 | 激励视频 | 查看模板详细讲解 |
| 高清卡片导出 | 激励视频 | 每日 3 次免费，超出解锁 1 次 |
| 摘抄本导出 | 激励视频 | 导出 PDF/TXT 解锁 |
| 开屏广告 | 开屏 | APP 冷启动 |
| 首页底部 | Banner | 常驻，不遮挡核心 |

**设计原则**：所有广告均为「解锁权益」模式，用户自主选择是否观看。

### 4.2 会员体系

| 档位 | 价格 | 权益 |
|---|---|---|
| 月卡 | 6 元 | 全素材自由看、无广告、卡片无水印 |
| 季卡 | 15 元（5 元/月） | 同月卡 |
| 年卡 | 48 元（4 元/月，主推） | 同月卡 + 离线下载 + 无限导出 |

---

## 五、冷启动与运营

### 内容冷启动策略

1. **自动采集 + 审核通过**：首次部署跑 seed 入库 50+ 采集源，手动导入 chinese-poetry（唐诗+宋词 ~2000 首），批量审核通过后即可上线
2. **AI 金句批量提取**：审核通过的原文丢给 AI 提取金句（豆包/DeepSeek），SimHash 去重后过审
3. **自建金句 + 原文**：运营后台手动录入经典素材补短板

### 内容更新节奏

- **自动部分**：Cron 定时采集 + AI 提取（配置好后全自动）
- **人工部分**：每周审核 1-2 次，过审后素材对用户可见
- **考前热点**：考前手动录入热点主题素材包

---

## 六、一期 MVP 完成度

| 模块 | 状态 | 说明 |
|---|---|---|
| 素材库浏览/搜索/收藏 | ✅ 完成 | |
| 每日推荐 + 随机摘抄 | ✅ 完成 | |
| 金句原文 + 选段一键加金句 | ✅ 完成 | |
| AI 金句提取（多模型 + SimHash 去重）| ✅ 完成 | 含火山方舟/百炼/千帆/智谱/DeepSeek/Moonshot |
| GitHub Raw 直导入 | ✅ 完成 | 30s 超时 + 3 次重试 + 中文路径编码 |
| 定时采集调度（node-cron）| ✅ 完成 | 每个采集源独立 Cron，可启停/立即执行 |
| 分级去重（MD5 + SimHash）| ✅ 完成 | 双重比对（正式库 + 临时库） |
| 原文审核流程（待审/批量审核）| ✅ 完成 | 待审内容不对用户展示 |
| 会员体系 | ✅ 完成 | memberLevel 分级 |
| 卡片工坊 | ⚠️ 基础完成 | 待补高级模板 |
| 背诵打卡（艾宾浩斯）| ✅ 完成 | |
| 作文模板 | ⚠️ 基础完成 | 待补更多模板 |
| 移动端 Flutter App | ⚠️ 骨架完成 | 功能逐步补齐 |
